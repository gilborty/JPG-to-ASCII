#include "Converter.hpp"

Converter::Converter( const std::string inputImagePath )
    : m_inputImageFilePath( inputImagePath )
{
    if( !m_inputImageFilePath )
    {
        throw std::
    }
     
}